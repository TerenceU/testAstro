---
interface Props {
  error: unknown;
}

const { error } = Astro.props;
---

PORCA MADOIA

errore: {error}
<br />

<!-- HTMX Butto to home -->
<button
  hx-get="/"
  hx-target="body"
  hx-swap="innerHTML"
  class="bg-blue-500 text-white font-bold py-2 px-4 rounded"
>
    Go to Home
</button>